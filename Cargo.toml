[package]
name = "cmake-tui-tool"
version = "1.0.0"
edition = "2024"

[dependencies]
cursive = { version = "0.21.1", features = ["crossterm-backend", "toml", "markdown"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
